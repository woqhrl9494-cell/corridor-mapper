# TSRI-S 풀이기 비교 결과 — 이전 근사 생성기

**이 문서는 정반사 생성부 수정 전의 과거 실행 기록이다. 현재 생성기의 검증 결과는 [REFLECTION_VALIDATION.md](REFLECTION_VALIDATION.md)를 참고한다. 아래 수치를 수정 후 성능으로 사용하지 않는다.**

실행일: 2026-09-09T07:22:11.404Z. 환경: darwin / arm64, Node v26.7.0. 전체 실행 117.3 s.

10개 seed (1001–1010), Corridor / Torus, 창 11 / 21 / 41개, 산란 OFF / ON의 120개 조합을 각각 100프레임 (5 s) 실행했다. 차량 8대, 거리 잡음 표준편차 0.1 m, 차량별 공유 위치 bias 표준편차 0.1 m, 속도 5 m/s, 간격 0.05 s, 풀이 간격 10프레임, GLS, 반복 상한 60을 사용했다. 설정은 결과를 보고 조정하지 않았다.

**세 풀이기의 수렴 결과는 대체로 같았다. 현재 거리 연결과 초기화, 기존 근사 반사 생성기를 포함한 전체 파이프라인은 접점을 충분히 정확하게 복원하지 못했다.** 수치적 수렴과 낮은 거리 잔차를 지도 복원 성공으로 보고하면 안 된다.

## 정반사 입력에서의 비교

수치는 10개 seed의 요약값 평균이다. 시간은 공통 준비를 제외하고 모든 초기 후보의 풀이와 품질 계산을 포함한다. 표에서 생략한 seed 수준 95% 신뢰구간과 paired 차이는 JSON에 모두 저장했다.

| 환경 | 창 / 지연 | 접점 RMSE, LM (m) | 거리 잔차, LM (m) | 지도 채택률, LM | LM 시간 (ms) | TR-GN 시간 (ms) | VP + LM 시간 (ms) |
|---|---|---:|---:|---:|---:|---:|---:|
| corridor | 11 / 0.25 s | 7.530 | 0.076 | 0.00% | 0.066 | 0.062 | 0.056 |
| corridor | 21 / 0.50 s | 7.563 | 0.093 | 5.68% | 0.112 | 0.118 | 0.103 |
| corridor | 41 / 1.00 s | 7.022 | 0.139 | 17.50% | 0.445 | 0.419 | 0.332 |
| torus | 11 / 0.25 s | 7.690 | 0.065 | 1.62% | 0.100 | 0.096 | 0.084 |
| torus | 21 / 0.50 s | 7.612 | 0.077 | 11.04% | 0.171 | 0.174 | 0.146 |
| torus | 41 / 1.00 s | 7.695 | 0.150 | 14.93% | 0.472 | 0.503 | 0.404 |

전체 120개 실행에서 방법 간 seed 요약 접점 RMSE 차이의 최댓값은 0.000302 m였다. 정반사 조건의 수렴률은 약 99.8–100%였으나, 보류 포함 접점 RMSE는 약 7.0–7.7 m였다. 이 차이는 숫자 풀이의 수렴이 접점의 올바른 선택을 보장하지 않음을 보여준다.

**채택한 점만 봐도 오차가 작다고 할 수 없다.** Corridor 창 21 / 41의 seed 평균 채택 RMSE는 각각 5.668 / 6.886 m, Torus 창 21 / 41은 5.775 / 6.125 m였다. 현재 공분산과 모호성 검사로 모든 잘못된 연결 또는 초기화 오류가 걸러지는 것은 아니다. 채택 표시는 reference gate 통과를 뜻하며 참 벽점임을 보장하지 않는다.

## 계산 시간의 해석

정반사 6개 환경 / 창 조합에서 VP + LM의 평균 풀이 시간은 LM보다 약 9–25% 짧았다. 이 실행의 관측값이며 하드웨어 또는 브라우저 전반의 고정 속도 향상으로 주장하지 않는다. TR-GN은 일부 조합에서 빨랐고 다른 조합에서는 느렸다. 짧은 실행에서는 JIT, garbage collection, 다른 검증 작업의 영향이 있을 수 있다. 브라우저에서 반복 비교를 다시 실행하면 해당 환경의 paired 결과가 나온다.

## 산란과 실패

산란 ON의 60개 실행은 편향 미보정 스트레스 시험이다. 거리 전처리가 정반사 기준 거리를 복원한 것으로 취급하지 않았으므로 지도 채택률은 설계상 0이다. 세 풀이기의 후보와 수렴, 잔차와 접점 오차는 계속 비교했다. 이를 “산란에서도 벽 복원 성공”으로 인용하지 않는다.

총 비교 대상은 40146개 시간창이다. 초기화 실패는 별도로 기록하며 세 방법 모두 같은 창을 받는다. 초기화된 각 창은 세 방법으로 풀고, 최적화 실패는 성공률에서 제외하지 않는다.

다음 검증은 별도로 필요하다.

- 기존 다각선 / 근사 반사기의 정상성 오차와 TSRI 시간 근사 오차 분리
- 거리 가지 연결 오류와 초기 후보의 다중해 분리
- 창 길이별 식별 가능성과 3차 이상 잔여항의 균형
- 실제 산란 또는 병합 거리 추정기의 편향 / 상관 모델

## 구현 검사

- 새로운 수치, 인과성, Worker 테스트 21개 통과
- 기존 산란 생성 테스트 5개, 벽 평가 테스트 5개 통과
- 보존한 이전 추정기 회귀 테스트 31개 통과. 현재 HTML은 이전 추정기를 실행하지 않음
- 원본 `5b03dc7` 대비 3개 조건, 15개 프레임의 벽 좌표 / 차량 위치 / 거리 / 반사점 일치
- jsdom에서 초기화, 40프레임 실행, 비교표, CSV 처리, Torus 전환, 산란 설정, 병합 보류, Reset 확인
- Worker의 실제 계산, 반복 비교, 취소, 내보내기 확인
- JavaScript 문법과 git diff 공백 검사 통과

jsdom은 DOM 실행 검사이며 실제 브라우저의 화면 배치, 다운로드 파일 생성, 설치 환경 성능 검증을 대신하지 않는다. 로컬 미리보기는 HTTP 200 응답을 확인하고 열었다.

## 재현 파일

- [시뮬레이터](index.html)
- [수식 / 가정 / 복잡도 / 평가 범위](TSRI_METHOD.md)
- [120개 실행의 설정, 원자료, CI와 소스 SHA-256](tsri_validation.json)
- 실행: `node benchmark_tsri.js 10 100 tsri_validation.json`
