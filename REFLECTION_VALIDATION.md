# 정반사 생성부 수정과 검증

기존 벽 좌표와 차량 운동을 유지하고 정반사점 계산과 차폐 검사를 수정했다. 생성 모델 식별자는 `specular-segment-v2`이다. TSRI의 목적함수, 세 풀이기, 초기화, 거리 연결, 공분산과 채택 기준은 수정하지 않았다.

## 변경 사항

- 선분 중점에서 25회 경사하강 후 cosine 차이로 통과시키던 계산을 선분의 정확한 평면 정반사 해로 교체했다.
- 모든 선분에서 물리적으로 가능한 후보를 찾고 가시성을 검사한 뒤, 벽별 최대 4개 경로 제한을 총길이가 짧은 순으로 적용한다. 이 개수 제한은 기존 설정이며 신호 세기나 피크 검출 모델은 아니다.
- 반사면의 같은 쪽에 Tx/Rx가 있고 접점이 선분 내부에 있어야 한다. 방향 벡터의 반사 법칙 잔차는 1e-10 이하만 허용한다. 선분 경계와 수치 허용오차 안의 grazing 접점은 생성하지 않는다.
- 선분 끝점에서의 차폐, collinear 겹침, 반사점 앞 5 cm 이내 차폐를 검사한다. 전파 구간 양 끝의 수치적 접촉만 제외하며 거리 허용오차는 1e-9 m에 좌표 roundoff 보정치를 더한 값이다.
- 산란 ON은 이 정확한 정반사 기준점 주변에 기존 경로 수 / 벽면 위치 분산 모형을 적용한다. 산란된 점에 정반사 법칙을 강제하지 않는다.
- 화면에 현재 정반사 기준 경로 수와 최대 방향 잔차를 표시한다. CSV와 JSON에는 생성 모델 식별자를 기록한다.

## 수학적 근거와 구현

선분 시작점 a, 단위 접선 T에 대한 Tx/Rx의 접선 좌표를 s_tx, s_rx, 같은 면에서 측정한 양의 수직 거리를 h_tx, h_rx라 둔다. 대칭점 방법의 접선 좌표는 다음과 같다.

```text
s* = s_tx + h_tx / (h_tx + h_rx) * (s_rx - s_tx)
x*(t) = a + s* T
rho(t) = ||x*(t) - p_tx(t)|| + ||x*(t) - p_rx(t)|| + epsilon(t)
```

길이와 위치는 m, 시간은 s, 방향 잔차는 무차원이다. 각 시각의 실제 반사점을 먼저 계산하고 마지막에 거리 잡음을 더한다. 생성기에는 TSRI의 b2나 고정 접점 시간 근사를 사용하지 않는다. 입력 형상과 반사점 참값은 생성 / 평가 영역에만 있고 추정기 입력 whitelist는 그대로다. 이전 감사의 평가 객체 공유 문제를 이번 변경으로 해결했다고 주장하지 않는다.

주요 코드: [echo_environment.js](echo_environment.js). 입력 / 출력과 복잡도는 `specularOnSegment`, `segmentBlocksLeg`, `simulatePair`에 설명돼 있다. 정적 선분 좌표계와 bounding box를 O(S) 공간에 미리 계산한다. 차량쌍별 정상점 검사는 O(S), V개 후보의 차폐 검사는 O(VS), 임시 후보 메모리는 O(V)이다. 차폐 검사 전 bounding box를 사용하지만 최악 복잡도는 유지된다.

## 재현 검사

실행일: 2026-09-09T07:58:04.794Z. darwin / arm64, Node v26.7.0. 비교 원본: `e7e55a4e94b1d807a11f12b6700fd61bb12f6765`. 소스 SHA-256과 원자료는 [reflection_validation.json](reflection_validation.json)에 있다.

복도 / 원형 통로, seed 7 / 31 / 42 / 87, 차량 8대, 각 100프레임, 산란 OFF, 거리 / 위치 잡음 0으로 생성기 자체를 비교했다. 검사한 벽 좌표, 선분, 실제 / 보고 차량 위치는 매 프레임 수정 전과 정확히 같았다. 생성되는 경로 수와 잡음 RNG 소비 순서는 변경됐으므로 수정 전후에 같은 관측값을 사용했다는 의미는 아니다.

| 환경 | seed | 수정 전 경로 수 | 수정 전 선분 내부 정상점 없음 | 수정 후 경로 수 | 수정 후 정상점 없음 | 수정 후 최대 접선 잔차 |
|---|---:|---:|---:|---:|---:|---:|
| corridor | 7 | 19,930 | 15,790 | 5,219 | 0 | 2.220e-15 |
| corridor | 31 | 16,199 | 12,979 | 5,295 | 0 | 2.056e-15 |
| corridor | 42 | 19,644 | 15,339 | 5,330 | 0 | 1.776e-15 |
| corridor | 87 | 17,316 | 13,914 | 5,499 | 0 | 2.220e-15 |
| torus | 7 | 21,236 | 15,680 | 5,627 | 0 | 3.997e-15 |
| torus | 31 | 18,152 | 13,175 | 5,370 | 0 | 4.996e-15 |
| torus | 42 | 21,140 | 15,269 | 5,916 | 0 | 5.329e-15 |
| torus | 87 | 20,928 | 15,159 | 5,914 | 0 | 3.331e-15 |

수정 후 총 44,170개 경로에서 정상점이 없는 출력은 0건이었다. 실제 출력점이 속한 선분의 접선을 독립적으로 다시 계산했으며, 생성기에서 보고한 잔차를 재사용하지 않았다. 통과 기준은 1e-10이고 관측 최대 잔차는 5.329e-15이다. 이 비율은 큰 거리 오차 비율이나 추정 실패 확률을 뜻하지 않는다.

- 자동 검사 26개 통과: 500개 회전 선분에서 독립 미분 이분법과 해석해 비교, Tx/Rx 교환, 알려진 잘못된 반사 사례, 선분 밖 / 반대면 / 경계 / 퇴화 입력, 차폐, 복도 / 원형 통로의 산란 기준점, 수치 풀이기, 입력 격리와 Worker.
- 기존 산란 모형 검사 5개와 벽 평가 지표 검사 5개 통과.
- Git 원본 5b03dc7의 HTML을 jsdom에서 직접 실행해 3조건 / 15프레임의 벽과 차량 위치 일치 확인. 수정 전 856개, 수정 후 219개 경로로 관측 변화 확인.
- jsdom에서 40프레임 실행, 세 방법 표, CSV 처리, Torus / 산란 전환, 미보정 거리 보류, Reset 통과.
- 로컬 실제 브라우저에서 40프레임 / 57개 추정창을 실행해 세 풀이기 표와 정반사 방향 잔차 표시 확인.

## 수정 후 TSRI 결과의 범위

아래는 seed 42, 차량 8대, 관측창 21개, stride 10, 각 100프레임에서 LM의 진단 결과다. 세 풀이기 모두 같은 관측창을 처리했고 유한한 위치 / 목적함수를 반환했다. 임계값을 이 결과에 맞춰 조정하지 않았다.

| 환경 | 거리 / 위치 표준편차 (m) | 추정창 수 | 채택 수 | 채택 중 접점 오차 > 0.4 m | 전체 접점 RMSE (m) |
|---|---:|---:|---:|---:|---:|
| corridor | 0.1 | 191 | 31 | 21 | 7.637 |
| corridor | 0 | 187 | 0 | 0 | 7.849 |
| torus | 0.1 | 293 | 115 | 78 | 7.625 |
| torus | 0 | 283 | 0 | 0 | 8.833 |

정반사 생성부의 조건 위반은 제거했지만, 접점 역산의 큰 오차와 잘못된 채택은 남아 있다. 0 잡음에서 채택 0건도 접점 복원 성공이 아니다. 현재의 거리 연결, 다중해 판정, 시간 근사 / 공분산 모델을 별도로 검증해야 한다. 이전 감사의 전체 실패를 생성기 문제 하나로 설명할 수 없다.

기존 벽은 선분 연결 형상이다. 각 선분 내부의 정확한 정반사를 생성해도 접점이 다른 면으로 전환되는 순간에 매끄러운 시간 가지가 보장되지는 않는다. 매끄러운 연속벽 실험과 모서리 전환 실험의 구분, 산란 전처리의 실제 보정 성능은 이번 변경 범위 밖이다.

이번 8대 실험의 반사 생성 시간은 프레임 평균 복도 약 1.3–1.5 ms, 원형 통로 약 2.3–2.5 ms였다. 이는 현 Mac의 Node에서 생성만 측정한 진단값이며 추정, 화면 렌더링, 통신 / 하드웨어 지연을 포함한 실시간 성능 보장이 아니다.

## 실행 명령

```sh
node --test tests/*.test.js
node diffuse_path.test.js
node wall_metrics.test.js
node tests/validate_reflection.js reflection_validation.json
# jsdom 27 이상이 설치된 환경:
node tests/environment_compatibility.js
node tests/jsdom_smoke.js
python3 -m http.server 8877 --bind 127.0.0.1
```
